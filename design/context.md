# Matryoshka Zig — Context Entry Point

## Writing rules
- Short intro, then bullets. Like staccato music.
- One fact per bullet.
- No prose paragraphs with comma-separated lists.

API reference: [matryoshka-api-reference-033.md](matryoshka-api-reference-033.md) — signatures, types, error sets, cancel contract, PolyHelper (+ toPoly/fromPoly/fromSlot + create/destroy + no_create_destroy), slot-based programming, cooperative cleanup patterns, tag identity, infra transport patterns, invariants, thread-safety, complexity, receiveResult/getWaitResult, mailbox.wakeUpAll — dependency-ordered (DOC 9 + DOC 10): nothing is used before it is introduced; generic std.Io material lives in a trailing Addendums/Io 101 section; humanized (DOC 18): no "ownership" framing, staccato throughout; `ItemHandle` naming (API 4, supersedes `NodeHandle`); New Mindset (022): Master connected to `io.concurrent()` up front, not left implicit; INTR 7 (023): "Pool is not storage" stated up front, `put`'s four hook-driven outcomes documented, no-fixed-sequence-guarantee caveat added; INTR 7 staccato scan (024): prose paragraphs converted to bullets, no content change; thread audit (025): worker-finish-signal pattern's stale "spawns a worker thread"/"joins the thread" language corrected to io.concurrent/future-await; API 5c (026): `on_put` composite-items return value (`?std.DoublyLinkedList`) documented; API 6 (027): PolyHelper accessors renamed to `fromNode`/`fromSlot` (+ `must` variants), new `moveFromSlot` takes the item out of a Slot — inspection leaves the Slot full, extraction empties it; API 7 (028): `toNode` added — the outbound accessor, inverse of `fromNode`, cannot fail, no `must` variant; `poly` is no longer touched by hand outside `src/`; API 8 (029): `ItemList` documented — the toolkit's list type, returned by `receive_batch`/`close`, taken by `put_all`, carried by both `PoolHooks`; `popFirst` yields `ItemHandle` and calls `reset`, so `@fieldParentPtr` is gone from application code; API 11 (033): `fromNode`/`mustFromNode`/`toNode` renamed to `fromPoly`/`mustFromPoly`/`toPoly` — `PolyNode` embeds `node: std.DoublyLinkedList.Node`, so the old names collided with the std link field; the Slot accessors keep their names

Manifesto: [matryoshka-manifesto-005.md](matryoshka-manifesto-005.md) — persuasion-first mindset doc (DOC 11 + DOC 12 plain-language pass): one constraint, Master is an Io task that follows the Matryoshka rules, four fundamental concepts, Io hidden behind Mailboxes; consolidates README + matryoshka-tk-model + matryoshka-master + master-Tk; supersedes matryoshka-manifesto.md, -002, -003, and -004; New Mindset (004): task-world diagram replaces the old floating-role diagram; New Mindset (005): dropped the "hybrid car" analogy, replaced with Io-creates-tasks/Matryoshka-lives-inside framing

Architecture: [matryoshka-architecture-004.md](matryoshka-architecture-004.md) — why matryoshka exists, concept progression, flows, layers; New Mindset (003): "ownership"/"owner" language replaced with "place"/"hold" (an object sits in exactly one place at any moment), "execution context" replaced with "task"

Architecture foundation: [matryoshka-architecture-foundation-4-004.md](matryoshka-architecture-foundation-4-004.md) — deep foundation doc: four layers (Hold/Movement/Lifecycle/Coordination), core concepts, concurrency contract, infrastructure as items, design decisions; New Mindset (002): "ownership" language replaced with "hold"/"holder"/"held" throughout (matches the existing HELD state name), "execution context(s)" replaced with "task(s)"; INTR 7 (003): "Pool is storage" framing replaced with "Pool is not storage — it is a backpressure signal for reuse"; INTR 7 staccato scan (004): prose paragraphs converted to bullets, no content change

New Mindset: [matryoshka-new-mindset-001.md](matryoshka-new-mindset-001.md) — Io creates tasks via `io.concurrent()`; a Master is an Io task that follows the Matryoshka rules, not a free-floating role; source of truth driving the README/manifesto/patterns/API-reference/building-blocks rewrite

Latest context: [collected-context-005.md](collected-context-005.md) — project state only

Status: [STATUS.md](STATUS.md) — Sources of Truth pointers + Session Log; latest entry: API 7 planned (PolyHelper outbound accessor `toNode`), CANDIDATES dropped

Thinking model: [matryoshka-model-006.md](matryoshka-model-006.md) — the who-holds-it mantra, three-category model, story structure, pool items are empty containers, when to allocate a Master

Rules: [rules-036.md](rules-036.md) — coding, doc, and process rules (+ example completeness rule + Master pattern rule + Observable by human rule + step function parameter rule + structural extraction signals + rules audit checklist item + Description as code rule + descriptive entry-point name rule (plain snake_case, not a quoted identifier — DOC 17) + example doc comment is file-level `//!`, not `///` on the entry point, and ASCII diagrams are fenced code blocks (DOC 17b/17c) + src/ doc comment rule + src/ terminology rule + src/ header staccato standard + scan verification rule + first-declaration doc-stub rule (DOC 18c) + mkdocs blank-line-before-list rule, now script-enforced via `kitchen/tools/fix_md_lists.sh` (rules-018, rules-023) + Handle naming rule: `ItemHandle` supersedes `NodeHandle`, short form `ih` (was `nh`), bare `handle` is acceptable shorthand (API 4) + Doc-generation module size rule updated: example-autodoc targets removed, hand-organized examples catalog instead (DOC 20) + examples-catalog nav sync rule: `examples/`/`stories/` changes must update `kitchen/mkdocs.yml` nav + group pages + "pitch" added to the AI-sh/banned word list (rules-022) + New Mindset banned words added: object model, execution context, execution model, programming model, paradigm, mindset, ownership, gained — usable in owner/agent conversation, banned from documentation and code comments; `ownership` broadens the prior src/-only rule (rules-012/013) to all docs and comments (rules-024) + Markdown hard-break rule: two-line staccato effect needs a blank-line paragraph split or two trailing spaces, now script-enforced via `kitchen/tools/fix_md_hardbreaks.sh`, hooked into `build_site.sh`/`preview_site.sh` (rules-025) + "wire"/"wired"/"wires"/"wiring" added to the AI-sh/banned word list (rules-026) + Accessor naming rule (API 6): inspection is `fromNode`/`fromSlot` and never empties a Slot, extraction is `moveFromSlot` and always does, no `As` suffix, a mutating helper gets no `must` variant (rules-027) + three more banned words added to the AI-sh/banned word list (rules-028) + "underneath" and "on purpose" added to the banned word list (rules-030) + API 8 (rules-029): the reset invariant is now a property of `ItemList`, not a rule the developer carries; `*std.DoublyLinkedList.Node` != `*PolyNode` recorded as a language fact with the langref citation, `@fieldParentPtr` confined to `src/polynode.zig` and the raw-link tests; DISPATCH 1 (rules-036): every dispatch chain ends with a final branch, and no `switch` over tags — a prong needs a comptime value and a tag is a linker-assigned address

Patterns: [patterns-024.md](patterns-024.md) — unified pattern and idiom catalog (DOC 13 + DOC 14): slot and transfer idioms, PolyNode, Mailbox, Topology patterns (Request-Response, Pipeline, Fan-In, Fan-Out), Pool, Futures, Select, Group, cancellation, graceful shutdown, Master patterns — one entry per concept, absorbs the api-reference pattern idioms and the Odin-docs audit findings; New Mindset (013): `Thread.spawn` removed as an accepted task-creation option, `io.concurrent()` only; INTR 7 staccato scan (014): intro prose converted to bullets, no content change; thread audit (015): worker-finish-signal pattern's stale thread-join language corrected to io.concurrent/future-await, stale api-reference-022.md cross-references updated to -025.md; EXMPL 5c (016): "Receive router — one registration, many events" added under Io.Select patterns, companion cross-reference corrected to rules-026.md; API 6 (017): `moveFromSlot` added to the transfer idiom, "Slot identification" section renamed and given the inspection-vs-extraction split; API 7 (018): "Stack item into the toolkit" idiom added for `toNode`, with an explicit do-not on round-tripping a statically-typed item; API 8 (019): "Walk a batch — ItemList" added, "Stack item into the toolkit" gained its list half; DISPATCH 1 (024): "Polymorphic dispatch" split into item-first and tag-first, plus the last-branch rule and the no-switch-over-tags fact

Plan: [matryoshka-tk-implementation-plan-050.md](matryoshka-tk-implementation-plan-050.md) — slim state-only plan; rules live in rules-034.md; API 5 (Composite Items) DONE; EXMPL 5 (receive router — example + pattern docs) DONE, 169/169 tests; MDFIX DONE (`kitchen/tools/fix_md_hardbreaks.sh`, rule in rules-026.md); API 6 (PolyHelper accessor naming) DONE, 170/170 tests; API 7 (PolyHelper outbound accessor `toNode`) DONE, 171/171 tests — 7a impl+tests, 7b examples, 7c docs, 7d `src/polynode.zig` doc-comment fixes (header typos + ItemHandle-vs-\*PolyNode layer vocabulary; `fromNode`/`toNode`/`isIt` signature changes considered and rejected); API 8 (ItemList — the `std.DoublyLinkedList` boundary closed) DONE, 175/175 tests — 8a design doc (`item-list-006.md`, 25 questions answered), 8b type + scenarios 100-103, 8c one atomic migration, 8d docs; API 7e closed as superseded; API 9 "intrusive safety" DONE 2026-07-30, 177/177 tests — `appendFromSlot`/`prependFromSlot` empty the Slot themselves and all four call sites migrated (prevention, Q31); `tests/layer1_itemlist.zig` added, scenarios 100-103 moved from `layer1_polynode.zig` plus new 104-105 (Q29); `ItemList._holds`, a private O(n) walk under runtime safety, asserts on all four inserts — it asks the container, not the item, so it sees the list of one `is_linked` cannot, and `mailbox.send`/`pool.put` inherit it through the same methods (Q34 C); `concat` asserts `self != other` (Q28); `is_linked` keeps its name and all seven asserts, doc comment corrected to "has neighbours", rules entry "The neighbour check" in rules-034.md, three test comments fixed (Q27, Q33); misuse cases 1 and 5 stay open — an item in a *different* list is not reachable from `self` and `PolyHelper.destroy` holds a Slot, the price of Q26 = D; docs to rules-034, patterns-021, api-reference-031, matryoshka-model-006 (companion links only), item-list-007, and the kitchen polynode/patterns pages; step 0 (happens-before invariant) shipped first the same day in rules-033.md and matryoshka-model-005.md; CANDIDATES dropped, owner's decision; API 10 "ItemList completion" DONE 2026-07-31, 182/182 tests — prompted by an external review of `src/polynode.zig`; `remove`, `popLast`, `first`, `last`, `insertBefore` added, reversing item-list-007 §2.3's "first real call site" rule because a missing `remove` sends callers through `_list` and hands them the `polynode.reset` obligation; `iterate` renamed to `iterator` (breaking, no shim, six in-repo call sites); `concat` gains `if (self == other) return;` — the Q28 assert is `unreachable` outside safety builds and `concatByMoving` rings the items then clears the header, losing the whole list; every insert now asserts `!is_linked` as well as `!_holds`, widening Q26 = D — the two checks are blind to opposite cases, and misuse case 1 is now partly covered while case 5 is not; `moveFromList` asserts its std header is consistent; owner's instruction "DoublyLinkedList checks nothing, ItemList should check everything"; docs to item-list-008 §12, api-reference-032, patterns-022, task1-tests-004

Storytelling: [../kitchen/docs/matryoshka-storytelling-001.md](../kitchen/docs/matryoshka-storytelling-001.md) — storytelling philosophy and rhythm rules (Discussion, SRS, Translation, Central Insight)

Docs plan: [matryoshka-tk-docs-plan-015.md](matryoshka-tk-docs-plan-015.md) — documentation work plan (mkdocs + autodocs, tofu audit, iterative DOC stages, top-down site skeleton + first Concepts story + Building Blocks topics: rule/pattern pairing, four core concepts + API reference re-partitioning + manifesto-002 + pattern catalog unification + Odin-docs pattern audit + API reference humanization + first-declaration doc-stub autodoc fix + example autodoc removal + hand-organized examples catalog (DOC 20))

Docs tooling approach: [docs-tooling-approach-002.md](docs-tooling-approach-002.md) — content-authoring method for DOC stages (audit-first parallel source review, triage ad-hoc dumps, narrow top-down scoping)

Kitchen notes: [../kitchen/notes.md](../kitchen/notes.md) — running notes on `kitchen/` tooling and generated content (not versioned, edit in place); currently: which `kitchen/docs/` files are generated vs. hand-authored

Tests (Layers 1-3): [task1-tests-005.md](task1-tests-005.md) — 84 scenarios (Layer1: 1-20 + 100-110, Layer2: 26-52, Layer3: 63-88), correctness/edge cases/contract violations; API 8 added 100-103 for `ItemList`; API 9 added 104-105, registered in -004; API 10 added 106-110 for `remove`, `popLast`, `first`/`last`, `insertBefore` and the `moveFromList` header check

Examples (Layers 1-4): [task1-examples-004.md](task1-examples-004.md) — 29 scenarios (Layer1: 21-25, Layer2: 53-62, Layer3: 89-92, Layer4: 17-24, 95-96), index only — full description in each source file's `///` doc comment

Tests (Layer 4): [task2-tests-002.md](task2-tests-002.md) — 16 scenarios (1-16), worker lifecycle/shutdown/cancellation. All done.

Examples (Layer 4 + cross-layer): [task2-examples-005.md](task2-examples-005.md) — 48 scenarios (17-31, 32-41, 42-61, 62, 95-96), index only — full description in each source file's `///` doc comment

Scenarios (historical): [task1-scenarios-001.md](task1-scenarios-001.md), [task2-scenarios-001.md](task2-scenarios-001.md) — original unsplit sources
