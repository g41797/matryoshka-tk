<*
 Matryoshka, the C3 port — the toolkit.

 `import mtk` brings in everything `module mtk` declares.
 The border and the two containers are submodules of it.
*>
// [3tk: D1 to D16, R1 to R15, H0, H0b, H5 to H10, E6, V19, Part 17.1, Part 17.2]
module mtk;

const String VERSION = "0.2.0";
